# SSO and OIDC

Operator-facing surfaces should be fronted by an identity-aware proxy or ingress auth using OIDC.
Map IdP groups to admin roles and keep break-glass access tightly controlled and audited.
