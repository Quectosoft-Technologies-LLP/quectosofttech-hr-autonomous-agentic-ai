# Secrets Management

Use External Secrets or a cloud secret manager such as AWS Secrets Manager, GCP Secret Manager, HashiCorp Vault, or Azure Key Vault.
Do not store long-lived production secrets in `.env` files or static Kubernetes manifests.
